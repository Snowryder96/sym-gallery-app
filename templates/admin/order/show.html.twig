{% extends 'admin/base.html.twig' %}

{% block title %}Message{% endblock %}

{% block body %}
<section class="page py-5">
    <div class="container p-4">
      <div class="row">
        <div class="col-md-6">
          <h1 class="text-primary my-4">Commande n°{{order.id}}</h1>
          <h2 class="text-primary my-4">peinture: {{order.painting.title}}</h2>
          <p>De la part de : <strong>{{order.buyer}}</strong></p>
          <p>email : <strong>{{order.email}}</strong></p>
          <p>date d'envoie : <strong>{{order.createdAt|date('d/m/Y')}}</strong></p>
        </div>
        <div class="col-md-6">
          <img src="{{ asset('uploads/paintings/' ~ order.painting.paintingFilename )}}" class="w-100" alt="{{order.painting.paintingFilename}}">
        </div>
      </div>
    </div>
</section>
{% endblock %}