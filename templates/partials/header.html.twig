{% set currentPath = path(app.request.attributes.get('_route'),app.request.attributes.get('_route_params')) %}
<nav class="navbar navbar-expand-lg navbar-dark bg-primary-dark">
	<img src="{{ asset("images/logo.png") }}" class="logo mr-3" alt="logo">
	<a class="navbar-brand" href="{{ path('home') }}">Patricia Masuy Art</a>
	<button class="navbar-toggler collapsed" type="button" data-toggle="collapse" data-target="#navbarColor02" aria-controls="navbarColor02" aria-expanded="false" aria-label="Toggle navigation"><span class="navbar-toggler-icon"></span></button>
	<div class="navbar-collapse collapse" id="navbarColor02" style="">
		<ul class="navbar-nav mr-auto">
				<li class="nav-item"><a class="nav-link text-white pb-2" href="{{ path('home') }}">
					<i class="fas fa-home"></i>
					&nbsp;Accueil
					</a></li>
						<li class="nav-item"><a class="nav-link text-white pb-2" href="{{ path('painting_index') }}">
						<i class="fas fa-image"></i>
						&nbsp;Portfolio
						</a></li>
						<li class="nav-item"><a class="nav-link text-white pb-2" href="{{ path('contact') }}">
						<i class="fas fa-user"></i>
						&nbsp;Contact
						</a></li>
		</ul>
		
		<ul class="navbar-nav ml-auto">
		
		{% if app.user  %} 
			<li class="nav-item dropdown">
				<a href="#" class="nav-link dropdown-toggle text-white" data-toggle="dropdown" id="accountDropdownLink" >
					{{app.user.email}}
				</a>
				<div class="dropdown-menu dropdown-menu-right" aria-labelledby="accountDropdownLink">
					<a href="{{ path('admin_painting_new')}}" class="dropdown-item">
					<i class="fas fa-plus text-primary"></i>
					&nbsp;Importer une peinture</a>
					<a href="{{ path('admin_painting')}}" class="dropdown-item">
					<i class="fas fa-image text-primary"></i>
					&nbsp;Peintures</a>
					<a href="{{ path('admin_message')}}" class="dropdown-item">
					<i class="fas fa-envelope text-primary"></i>
					&nbsp;Messages</a>
					<a href="{{ path('admin_category')}}" class="dropdown-item">
					<i class="fas fa-ellipsis-v text-primary"></i>
					&nbsp;Categories</a>
					<a href="{{ path('admin_technic')}}" class="dropdown-item">
					<i class="fas fa-ellipsis-v text-primary"></i>
					&nbsp;Techniques</a>
					<div class="dropdown-divider"></div>
					<a href="{{ path('admin_index')}}" class="dropdown-item">
					<i class="fas fa-tools text-primary"></i>
					&nbsp;Panel admin</a>
					<div class="dropdown-divider"></div>
						<a href="{{ path('account_logout')}}" class="dropdown-item">
						<i class="fas fa-sign-out-alt text-primary"></i>
						&nbsp;Deconnexion</a>
				</div>
			</li>
		
		{% endif  %}
		</ul>
	</div>
</nav>
{% for label, messages in app.flashes %}            
<div class="container">
	<div class="alert alert-{{label}}">
		{% for message in messages %}                        
		<p>
			{{ message | raw }}
		</p>
	  {% endfor %}                
	</div>
</div>
{% endfor %}      