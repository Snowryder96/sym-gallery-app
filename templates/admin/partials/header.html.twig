<nav class="navbar navbar-expand-lg navbar-dark bg-primary">
	<img src="{{ asset("images/logo.png") }}" class="logo mr-3" alt="logo">
	<a class="navbar-brand" href="{{ path('home') }}">Patricia Masuy Art</a>
	<button class="navbar-toggler collapsed" type="button" data-toggle="collapse" data-target="#navbarColor02" aria-controls="navbarColor02" aria-expanded="false" aria-label="Toggle navigation"><span class="navbar-toggler-icon"></span></button>
	<div class="navbar-collapse collapse" id="navbarColor02" style="">
		<ul class="navbar-nav mr-auto">
			<li class="nav-item"><a class="nav-link" href="{{ path('admin_index')}}">
				<i class="fas fa-tasks"></i>
				Dashboard</a></li>
			<li class="nav-item"><a class="nav-link" href="{{ path('admin_painting')}}">
				<i class="fas fa-image"></i>
				Peintures</a></li>
			<li class="nav-item"><a class="nav-link" href="{{ path('admin_message')}}">
				<i class="fas fa-envelope"></i>
				Messages</a></li>
			<li class="nav-item"><a class="nav-link" href="{{ path('admin_category')}}">
				<i class="fas fa-ellipsis-v"></i>
				Categories</a></li>
			<li class="nav-item"><a class="nav-link" href="{{ path('admin_technic')}}">
				<i class="fas fa-ellipsis-v"></i>
				Techniques</a></li>
		</ul>
		<ul class="navbar-nav ml-auto">
			<li class="nav-item">
				<a href="{{ path('home') }}" target="_blank" class="nav-link">
					<i class="fas fa-eye"></i>
					Voir le site
				</a>
			</li>
		{% if app.user  %}  
			<li class="nav-item dropdown">
				<a href="#" class="nav-link dropdown-toggle" data-toggle="dropdown" id="accountDropdownLink" >
					{{app.user.email}}
				</a>
				<div class="dropdown-menu dropdown-menu-right" aria-labelledby="accountDropdownLink">
					<a href="{{ path('admin_painting_new')}}" class="dropdown-item">
					<i class="fas fa-plus text-primary"></i>
					&nbsp;Importer une peinture</a>
					<div class="dropdown-divider"></div>
					<a href="{{ path('account_logout')}}" class="dropdown-item">
					<i class="fas fa-sign-out-alt text-primary"></i>
					&nbsp;Deconnexion</a>	
				</div>
			</li>
		{% endif  %}
		</ul>
	</div>
</nav>
{% for label, messages in app.flashes %}            
<div class="container">
	<div class="alert alert-{{label}}">
		{% for message in messages %}                        
		<p>
			{{ message | raw }}
		</p>  {% endfor %}                
	</div>
</div>
{% endfor %}      